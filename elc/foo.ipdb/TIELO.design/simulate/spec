DESIGN(TIELO) + REFERENCE(AMI06N,AMI06P);
PORT(Y) + FEEDTHRU + NET_NUMBER(0);
PORT(VDD) + DIRECTION(INPUT) + SUPPLY1 + NET_NUMBER(1);
PORT(GND) + DIRECTION(INPUT) + SUPPLY0 + NET_NUMBER(2);
VECTOR(010) + ID(D0000) + MID(ML0000) + POWER();
END_OF_DESIGN;

